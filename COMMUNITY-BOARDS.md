# 커뮤니티 게시판 · 2026-09-08

- EPL, 라리가, 분데스리가, 세리에 A, 리그 1, 한국선수, K리그, 국가대표 게시판을 추가했다.
- 자유게시판은 유지한다. 기존 `posts`와 `pcmt` 자료를 삭제하거나 이동하지 않는다.
- 신규 글은 `posts/{postId}.board`에 게시판 ID를 저장한다. 자유게시판은 기존 글과 호환되도록 해당 필드를 비운다.
- 각 게시판은 `#board-epl`, `#board-national` 같은 주소를 갖는다.
- 게시판별 최근 100개 글을 요청하며, 이전 요청이 늦게 도착해도 현재 목록을 덮어쓰지 않는다. 댓글은 표시 대상 게시글의 경로만 조회한다.
- 채팅 UI와 송수신 코드를 제거했다. 기존 채팅 데이터 자체에는 접근하거나 삭제하지 않았다.
- 글쓰기 중복 등록을 막고 게시판별 임시 작성 내용을 이동 중 보존한다.

검증: 카테고리·기존 글 호환·비동기 전환·쓰기 대상·중복 등록 테스트. 브라우저에서 실제 데이터 읽기, 빈 목록, 게시판 이동·새로고침과 작성 화면을 확인했다. 운영 DB에 테스트 글을 게시하지 않았다. 쓰기 요청은 모의 DB로 검증했다.

운영 참고: 데이터가 많아질 때 Firebase `posts` 규칙의 `.indexOn`에 `board`를 추가하면 카테고리 조회 성능을 개선할 수 있다. 기존 접근 권한은 변경하지 않았다.
